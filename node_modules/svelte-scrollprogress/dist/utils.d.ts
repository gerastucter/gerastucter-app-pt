/**
 * Debounces a function, ensuring it is only called once after a delay.
 *
 * @param func The function to debounce.
 * @returns The debounced function.
 */
export declare const debounce: <T extends (...args: any[]) => void>(func: T) => (() => void);
